# ===========================================================================
#
# game - app view
#
# ===========================================================================
define(["lib/backbone"], (Backbone)->
    class App extends Backbone.View
        #====================================
        #
        #Methods
        #
        #====================================
        initialize: ()->
            #Properties we'll use later
            return @

        render: ()->
            return @
    return App
)
