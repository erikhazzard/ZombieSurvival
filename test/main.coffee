chai = require('chai')
chai.should()
{Game} = require('../src/main.js')

describe('Game instance', ()->
    it('should have cells', ()->
        game = new Game()
        task1.name.should.equal('')
    )
)
